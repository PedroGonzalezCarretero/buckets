import React from 'react';

type Props = {};

const page = async (props: Props) => {

    const users = await 


   return (
      <div>
         <h1 className='text-3xl'>List of all Users</h1>
      </div>
   );
};

export default page;
